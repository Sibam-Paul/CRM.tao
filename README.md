# theAlphaCRM
CRM for TheAlphaOnes
# Core libraries
npm install drizzle-orm postgres

# Development tools (for pushing schema updates)
npm install -D drizzle-kit

# supabase 
->  npm install @supabase/supabase-js @supabase/ssr

# setup 
-> npm i , npm run dev 